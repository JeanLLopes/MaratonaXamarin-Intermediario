<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MonkeyHubApp.MainPage2"
             Title="Página Inicial">
    <ContentPage.Content>
        
        <!--COLOCA UM ELEMENTO EMBAIXO DO OUTRO-->
        <StackLayout>
            <Button Text="Pagina Sobre" Command="{Binding AboutCommand}"></Button>

            <Button Text="Clique AQUI Modal"></Button>
            
            <Button Text="Voltar Modal"></Button>
            
            <Entry Text="{Binding SearchTerm}"></Entry>
            <Button Text="Pesquisar" Command="{Binding SearchCommand}"></Button>

            
            <!--AQUI PODEMOS FAZER O BINDING DA LISTA-->
            <ListView ItemsSource="{Binding Resultados}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Title}" Detail="{Binding Body}"></TextCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
        
    </ContentPage.Content>
</ContentPage>